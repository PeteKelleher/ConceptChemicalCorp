<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="purchase.email_template_edi_purchase_done" model="mail.template">
            <field name="name">Purchase Order: Send PO</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="subject">Purchase Order Confirmation ${object.name or 'n/a' } from  ${object.company_id.name} </field>
            <field name="email_from">${object.create_uid.email_formatted | safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <div style="margin: 0px; padding: 0px;">
                        <p style="margin: 0px; padding: 0px; font-size: 13px;">
                            Dear ${object.partner_id.name},
                            % if object.partner_id.parent_id:
                                (${object.partner_id.parent_id.name})
                            % endif
                            <br/><br/>
                        Please find attached our Purchase Order <strong>${object.name}</strong>
                        for Product
                            % for line in object.order_line:
                                    <strong>
                                        ${line.product_id.name},
                                    </strong>
                            % endfor
                        </p>
                    </div>
                </div>
            </field>
        </record>
    </data>
</odoo>